apiVersion: v1
kind: Service
metadata:
  name: {{ include "customtomcatrepo11.fullname" . }}
  labels:
    {{- include "customtomcatrepo11.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "customtomcatrepo11.selectorLabels" . | nindent 4 }}
