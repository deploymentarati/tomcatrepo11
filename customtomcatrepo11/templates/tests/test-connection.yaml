apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "customtomcatrepo11.fullname" . }}-test-connection"
  labels:
    {{- include "customtomcatrepo11.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "customtomcatrepo11.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
